{
    "version": "2.0.0",
    "tasks": [
      {
        "label": "Clean & Build Expo Router for Netlify",
        "type": "shell",
        "command": "rm -rf node_modules .expo dist && npm install --legacy-peer-deps && npx expo export -p web",
        "problemMatcher": [],
        "group": {
          "kind": "build",
          "isDefault": true
        },
        "presentation": {
          "reveal": "always",
          "panel": "shared",
          "clear": true
        },
        "options": {
          "shell": {
            "executable": "/bin/zsh",
            "args": ["-l", "-c"]
          }
        }
      }
    ]
  }